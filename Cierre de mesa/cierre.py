from motivos import motivos
from funciones import *
 
radicado = int(input("Número del radicado de la solicitud: "))

motivo = int(input(f"\n1). Activación de Office.\n2). Computador lento\n> "))

user = str(input("Nombre del usuario: "))

user = user.capitalize()